enum PlaylistType {
  defaultPlaylistType,
  potPlayerPlaylistType,
}

enum DoubleClickAction {
  toggleWindowSize,
  playPause,
}

enum SeekMode {
  adaptive,
  fixed,
}

enum ActionContext {
  player,
  playlist
}